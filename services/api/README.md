# API service

Run from the repository root with `python -m seekandscore.api`. The canonical
ASGI application is `seekandscore.api:app`; this directory only marks the
deployment boundary from shared backend modules.
