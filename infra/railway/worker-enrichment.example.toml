[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docker/worker.Dockerfile"
watchPatterns = [
  "services/worker/**",
  "python/seekandscore/**",
  "config/**",
  "migrations/**",
  "pyproject.toml",
  "uv.lock",
  "infra/docker/worker.Dockerfile"
]

[deploy]
startCommand = "python -m seekandscore.worker --queues resolve,geo,market,score,deliver"
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
drainingSeconds = 60
