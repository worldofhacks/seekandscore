[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docker/worker.Dockerfile"
watchPatterns = [
  "services/worker/**",
  "python/seekandscore/**",
  "config/**",
  "migrations/**",
  "pyproject.toml",
  "uv.lock",
  "infra/docker/worker.Dockerfile",
  "scripts/runtime/**"
]

[deploy]
startCommand = "/app/scripts/runtime/database-role-entrypoint.sh unprovisioned python -m seekandscore.worker --queues resolve,geo,market,score,deliver"
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
drainingSeconds = 60
