[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docker/worker.Dockerfile"
watchPatterns = [
  "services/worker/**",
  "python/seekandscore/**",
  "config/**",
  "migrations/**",
  "pyproject.toml",
  "uv.lock",
  "infra/docker/worker.Dockerfile",
  "scripts/runtime/python-entrypoint.sh"
]

[deploy]
startCommand = "/app/scripts/runtime/python-entrypoint.sh python -m seekandscore.worker --queues resolve,geo,market,score,deliver"
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
drainingSeconds = 60
